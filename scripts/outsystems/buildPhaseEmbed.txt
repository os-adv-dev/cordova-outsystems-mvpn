# Type a script or drag a script file from your workspace to insert its path.\n# Type a script or drag a script file from your workspace to insert its path.\nexport TOOLKIT_DIR="$PROJECT_DIR/Tools"\nexport IPA_FILE_PATH="$IPAFILEPATH"\nexport EXECUTABLE_NAME="$EXECUTABLENAME"\n\nif [ ! -d $TOOLKIT_DIR/logs ]\nthen\n    mkdir $TOOLKIT_DIR/logs\nfi\n\n\n"$TOOLKIT_DIR/CGAppCLPrepTool" SetInfo -in "$SRCROOT/mdx/$EXECUTABLE_NAME.mdx" -out "$SRCROOT/mdx/$EXECUTABLE_NAME-exported.mdx" -embedBundle "$CONFIGURATION_BUILD_DIR/$EXECUTABLE_NAME.ipa"